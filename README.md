# Vector AI Landing — Инструкция по запуску

## 🚀 Быстрый старт

### 1. Установите Node.js (если не установлен)

Скачайте с официального сайта: https://nodejs.org/

Проверьте установку:
```bash
node -v
npm -v
```

### 2. Установите зависимости

Откройте терминал в папке проекта:
```bash
cd /Users/loveyou/Desktop/landings
npm install
```

### 3. Настройте Telegram бота

**⚠️ ВАЖНО: Отозовите старый токен!**

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/revoke`
3. Выберите вашего бота
4. Получите новый токен
5. Отправьте команду `/mybots` → выберите бота → Bot Settings → Edit Bot Info (если нужно)

**Обновите файл `.env`:**
- Откройте файл `.env` в редакторе
- Вставьте новый `TELEGRAM_BOT_TOKEN`
- Проверьте `TELEGRAM_CHAT_ID` (можно узнать у [@userinfobot](https://t.me/userinfobot))

### 4. Запустите сервер

```bash
npm start
```

Вы увидите:
```
🚀 Server running on http://localhost:3000
📬 Lead endpoint: POST http://localhost:3000/api/send-lead
```

### 5. Откройте сайт

Откройте файл `index.html` в браузере или используйте Live Server.

Заполните форму — заявка придёт в ваш Telegram!

---

## 📁 Структура проекта

```
landings/
├── index.html          # Главная страница (фронтенд)
├── server.js           # Сервер (бэкенд)
├── package.json        # Зависимости Node.js
├── .env                # 🔐 Токены (НЕ публикуйте в интернет!)
├── .gitignore          # Игнорируемые файлы
└── README.md           # Эта инструкция
```

---

## 🔒 Безопасность

- **Файл `.env` содержит секретные данные** — никогда не публикуйте его в GitHub
- Файл `.gitignore` уже настроен для защиты `.env`
- Токен бота теперь хранится на сервере, а не в HTML

---

## 🌐 Публикация в интернет

Для работы на хостинге вам понадобится:

### Вариант 1: VPS сервер (Selectel, Timeweb, Reg.ru)
1. Арендуйте сервер (~200-500₽/мес)
2. Установите Node.js
3. Загрузите файлы проекта
4. Запустите через PM2: `pm2 start server.js`

### Вариант 2: Render.com (бесплатно)
1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Укажите переменные окружения (TOKEN, CHAT_ID)

### Вариант 3: Railway.app (бесплатно)
1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Deploy from GitHub
4. Добавьте переменные окружения

---

## 🛠 Команды

```bash
npm start      # Запуск сервера
npm run dev    # Режим разработки
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте, что сервер запущен (`npm start`)
2. Проверьте консоль браузера (F12) на ошибки
3. Убедитесь, что токен в `.env` актуальный
4. Проверьте, что бот добавлен в чат (если чат групповой)
