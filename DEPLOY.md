# 🚀 Инструкция по деплою на Render.com

## Шаг 1: Загрузите проект на GitHub

1. Создайте новый репозиторий на GitHub: https://github.com/new
2. Назовите его, например: `vector-ai-landing`
3. Сделайте его **публичным** (для бесплатного тарифа)

### Загрузите файлы через терминал:

```bash
cd /Users/loveyou/Desktop/landings

# Инициализируйте Git (если ещё не инициализирован)
git init

# Создайте .gitignore (уже создан, но проверьте)
echo "node_modules/" > .gitignore
echo ".env" >> .gitignore
echo ".DS_Store" >> .gitignore

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit - Vector AI Landing"

# Добавьте удалённый репозиторий (замените YOUR_USERNAME на ваш логин GitHub)
git remote add origin https://github.com/YOUR_USERNAME/vector-ai-landing.git

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

---

## Шаг 2: Зарегистрируйтесь на Render

1. Перейдите на https://render.com
2. Нажмите **Get Started for Free**
3. Войдите через GitHub (рекомендуется) или email

---

## Шаг 3: Создайте Web Service

1. В личном кабинете нажмите **New +** → **Web Service**
2. Выберите **Connect a repository**
3. Найдите ваш репозиторий `vector-ai-landing` и нажмите **Connect**

---

## Шаг 4: Настройте сервис

Заполните поля:

| Поле | Значение |
|------|----------|
| **Name** | `vector-ai-bot` (или любое другое) |
| **Region** | Frankfurt (Germany) — ближе к России |
| **Branch** | `main` |
| **Root Directory** | оставьте пустым |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `node server.js` |
| **Instance Type** | `Free` |

---

## Шаг 5: Добавьте переменные окружения

В разделе **Environment Variables** добавьте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `TELEGRAM_BOT_TOKEN` | `8783014575:AAHGf9re0kBUwVVXDSEP8xs5WA9dSVpTsBg` |
| `TELEGRAM_CHAT_ID` | `807904356` |

⚠️ **Важно:** Вставьте ваш актуальный токен (не тот, что был в HTML!)

---

## Шаг 6: Запустите

1. Нажмите **Create Web Service**
2. Подождите 2-5 минут, пока Render построит и запустит сервис
3. Когда статус станет **Live**, скопируйте URL (например: `https://vector-ai-bot.onrender.com`)

---

## Шаг 7: Проверьте работу

1. Откройте ваш URL в браузере
2. Заполните форму заявки
3. Проверьте Telegram — должно прийти сообщение

---

## 🔧 Полезные команды

### Перезапустить сервис:
```bash
# В панели Render: Settings → Manual Deploy → Redeploy
```

### Посмотреть логи:
```bash
# В панели Render: Logs → Full Logs
```

---

## ⚠️ Важно знать

### Бесплатный тариф Render:
- ✅ Бесплатно навсегда
- ⏸️ Сервис «засыпает» после 15 минут бездействия
- ⏱️ Первое запрос после простоя занимает 30-50 секунд
- 🔄 После «пробуждения» всё работает нормально

### Как избежать «засыпания»:
1. Используйте **UptimeRobot** (https://uptimerobot.com/)
2. Создайте мониторинг вашего URL
3. Установите интервал проверки: 5 минут
4. Сервис будет постоянно активен

---

## 🎁 Бонус: Настройка своего домена

1. Купите домен (например, на reg.ru или namecheap.com)
2. В панели Render: **Settings** → **Custom Domains** → **Add Custom Domain**
3. Введите ваш домен
4. Настройте DNS у регистратора домена:
   - Type: `CNAME`
   - Name: `www`
   - Value: `vector-ai-bot.onrender.com`

---

## 📞 Что-то не работает?

1. Проверьте логи в панели Render
2. Убедитесь, что токен бота актуальный
3. Проверьте, что бот не заблокирован
4. Убедитесь, что chat_id правильный

---

## 🎉 Готово!

Ваш сайт теперь доступен по всему миру! 🌍

Пример: `https://vector-ai-bot.onrender.com`
